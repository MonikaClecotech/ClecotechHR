<div class="row show_ques">
  <% Survey.all.each do |survey| %>
    <div class="col-sm-2"> 
      <%= link_to question_form_questions_path(survey.id), class: "survey-button btn btn-lg btn-primary" do %>
         <%= survey.name %>
      <% end %>
    </div>
  <% end %> 
</div>
  